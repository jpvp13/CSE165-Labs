#include <iostream>
#include "projectile.h"
#include "App.h"

projectile::projectile(){
    // bullet.push_front(new RectChild(-0.1, -0.6, 0.3, 0.2, 1, 1, 1));

}


void projectile::shoot(){
    // for(std::deque<RectChild*>::iterator i = bullet.begin(); i != bullet.end(); i++){   //will iterate until the end of bullet
        // if((*i)->contains(mx,my)){  //will check to see if mx, my are contained where the bullet passes
            // bullet.push_front(new RectChild(mx, my + 0.5 , 0.1, 0.1, 0, 1, 0));
            //   std::cout << "X pos: " << mx << "and Y pos: " << my << std::endl;
        // }
    // }

      
    std::cout << "pewpew" << std::endl;
}